# Clauder Configuration
# Copy this file to .env and customize the values for your setup

# Coordinator service URL for remote iOS access
# This is required for the quickstart command to enable remote access via passcode
# Leave commented out to disable remote coordinator features
COORDINATOR_URL=https://claude-code-coordinator.team-ad9.workers.dev

# HTTP server port (default: 3284)
# Uncomment and change if you want to use a different port
# PORT=3284

# Additional configuration options:
# - All settings can also be set as environment variables
# - Environment variables take precedence over .env file settings